<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>반복문</title>
</head>
<body>

<script type="text/javascript">
    // 반복문
    // 이 코드는 for 반복문을 사용하여 1부터 10까지의 숫자를 반복하면서 출력하고, 
    // 그 숫자들을 더한 합계를 계산합니다.

    let sum = 0;  // 변수 sum은 1부터 10까지의 숫자를 더한 합계를 저장하기 위해 사용됩니다.
    let i;  // 반복문이 끝난 후에도 사용할 수 있도록 전역 범위에서 변수 i를 선언합니다.

    // for 반복문은 초기값(i=1), 조건식(i<=10), 증감식(i++)으로 구성됩니다.
    // i가 1부터 시작하여 10까지 증가하면서 반복문이 실행됩니다.
    // let 을 var 로 변경하면 에러를 없애고 출력 가능함.
    for(let i = 1; i <= 10; i++) {
        // i 값이 1에서 10까지 증가하는 동안, 매번 그 값을 출력합니다.
        document.write("i = " + i + "<br/>");
        
        // 각 반복에서 1을 sum에 더해 합계를 계산합니다.
        sum += 1;
    } // for문이 종료될 때, i는 11이 되어 반복문을 탈출합니다.

    // 반복문이 종료된 후 i는 11이므로, 탈출한 시점의 i 값을 출력합니다.
    document.write("<h3>탈출 i = " + i + "</h3>");

    // 최종적으로 계산된 sum 값을 출력합니다. 이 경우 sum에는 10이 더해졌습니다.
    // sum은 매 반복마다 1씩 증가했으므로, 결과적으로 1부터 10까지의 숫자가 더해져서 10이 됩니다.
    document.write("<h1>1부터 10까지의 합은 " + sum + "입니다.</h1>");

</script>
</body>
</html>


html
코드 복사
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>반복문</title>
</head>
<body>

<script type="text/javascript">
    // 반복문
    // 이 코드는 for 반복문을 사용하여 1부터 10까지의 숫자를 반복하면서 출력하고, 
    // 그 숫자들을 더한 합계를 계산합니다.

    let sum = 0;  // 변수 sum은 1부터 10까지의 숫자를 더한 합계를 저장하기 위해 사용됩니다.
    let i;  // 반복문이 끝난 후에도 사용할 수 있도록 전역 범위에서 변수 i를 선언합니다.

    // for 반복문은 초기값(i=1), 조건식(i<=10), 증감식(i++)으로 구성됩니다.
    // i가 1부터 시작하여 10까지 증가하면서 반복문이 실행됩니다.
    for(let i = 1; i <= 10; i++) {
        // i 값이 1에서 10까지 증가하는 동안, 매번 그 값을 출력합니다.
        document.write("i = " + i + "<br/>");
        
        // 각 반복에서 1을 sum에 더해 합계를 계산합니다.
        sum += 1;
    } // for문이 종료될 때, i는 11이 되어 반복문을 탈출합니다.

    // 반복문이 종료된 후 i는 11이므로, 탈출한 시점의 i 값을 출력합니다.
    document.write("<h3>탈출 i = " + i + "</h3>");

    // 최종적으로 계산된 sum 값을 출력합니다. 이 경우 sum에는 10이 더해졌습니다.
    // sum은 매 반복마다 1씩 증가했으므로, 결과적으로 1부터 10까지의 숫자가 더해져서 10이 됩니다.
    document.write("<h1>1부터 10까지의 합은 " + sum + "입니다.</h1>");

</script>
</body>
</html>

<!-- 코드 분석
for 반복문:

for(let i = 1; i <= 10; i++)는 1부터 10까지 i를 1씩 증가시키며 반복을 수행합니다.
반복 조건은 i <= 10이므로, i가 10까지 반복되고, i가 11이 되는 순간 조건이 거짓이 되어 반복문이 종료됩니다.
각 반복에서 document.write()를 사용하여 i의 값을 출력하고, sum += 1로 sum에 1씩 더합니다.
sum 변수:

반복문이 시작할 때 sum은 0입니다.
반복문이 실행될 때마다 sum += 1이 실행되므로, 최종적으로 sum은 10이 됩니다.
반복문 탈출 후 i:

for 반복문 안에서 선언된 i는 블록 범위 내에서만 유효합니다.
let i;로 전역 변수로 선언된 i는 for 블록 외부에서 참조되며, 반복문이 종료된 후에는 i = 11로 출력됩니다.
출력 결과:

반복문 내에서 i 값이 1부터 10까지 출력됩니다.
반복문이 종료된 후에는 i가 11이 되어 탈출 시의 i 값이 출력됩니다.
마지막으로 1부터 10까지의 합은 10입니다.가 출력됩니다. -->
