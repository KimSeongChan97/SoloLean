<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Upload</title>
</head>
<body>
	<h1>활성 / 비활성 구현하기</h1>
	<form name="form1">
		<!-- 업로드 체크박스: 사용자가 파일 업로드 기능을 활성화/비활성화할 수 있게 하는 체크박스 -->
		<input type="checkbox" id="upload_file_check" value="Y" />
		<!-- 파일 선택 input: 기본적으로 비활성화된 상태(disabled). -->
		<!-- 체크박스가 선택되면 활성화되고, 체크박스가 선택 해제되면 다시 비활성화됩니다. -->
		<input type="file" id="file_finder" disabled="disabled" />
		<!-- disabled 은 비활성화 -->
	</form>
	
<script type="text/javascript" src="http://code.jquery.com/jquery-3.7.1.min.js"></script>
<script type="text/javascript">
$(function(){
	// #upload_file_check 요소에 change 이벤트를 추가합니다.
	// 사용자가 체크박스를 체크하거나 체크 해제할 때마다 이 함수가 실행됩니다.
	$('#upload_file_check').change(function(){
		// 체크박스가 체크되었는지 여부를 확인하여 그 값을 isChk에 저장합니다.
		// $(this)는 현재 이벤트가 발생한 요소(즉, #upload_file_check)를 가리킵니다.
		// .is(':checked')는 체크 여부를 boolean 값(true/false)로 반환합니다.
		let isChk = $(this).is(':checked'); // false 여야 disable이 true가 되어야 함.
		
		// 파일 선택 요소인 #file_finder의 'disabled' 속성을 제어합니다.
		// prop() 메서드를 사용하여 속성 값을 설정하며,
		// !isChk는 isChk의 반대 값을 의미합니다. 
		// 즉, 체크박스가 체크되면(= isChk가 true이면) 파일 선택 요소가 활성화되고,
		// 체크박스가 체크 해제되면(= isChk가 false이면) 파일 선택 요소가 비활성화됩니다.
		$('#file_finder').prop('disabled', !isChk);
		// 예를 들어, isChk가 true일 경우 !isChk는 false가 되어 비활성화 속성이 제거됩니다.
		// 반대로, isChk가 false일 경우 !isChk는 true가 되어 비활성화 속성이 추가됩니다.
	});
	
	
});
</script>
</body>
</html>
