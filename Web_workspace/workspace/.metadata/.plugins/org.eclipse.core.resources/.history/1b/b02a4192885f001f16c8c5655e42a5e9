<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>회원가입시 인증번호</title>
</head>
<body onload="autoNo()"> <!-- 페이지가 로드될 때 자동으로 autoNo() 함수를 호출하여 인증번호를 생성합니다. -->
	<p>
		고객님의 인증번호는 <strong id="auth">00000</strong>입니다. <!-- 인증번호를 표시할 부분입니다. 초기 값은 "00000"으로 설정되어 있습니다. -->
	</p>
	
	<!-- "인증번호 새로 받기" 버튼을 클릭하면 refresh() 함수가 호출되어 페이지가 새로고침됩니다. -->
	<input type="button" value="인증번호 새로 받기" onclick="refresh()"/>	
	<!-- "네이버로 이동하기" 버튼을 클릭하면 goNaver() 함수가 호출됩니다. -->
	<input type="button" value="네이버로 이동하기" onclick="goNaver()"/>
	
	
<script type="text/javascript">
/* random 함수는 x와 y 사이의 랜덤한 정수를 반환합니다. */
function random(x, y) {
	return Math.floor(Math.random() * (y-x+1) + x); // x~y 사이의 난수 발생
	// Math.random()은 0 이상 1 미만의 난수를 생성합니다.
	// (y - x + 1)를 곱한 후, x를 더해 x와 y 사이의 난수를 만듭니다.
	// Math.floor()를 사용하여 소수점 이하를 버리고 정수로 반환합니다.
} 


/* autoNo 함수는 인증번호를 자동으로 생성하여 화면에 표시하는 함수입니다. */
function autoNo() {
	let value = ""; // 인증번호를 저장할 변수입니다.
		// 5자리의 난수로 이루어진 인증번호를 생성하기 위한 for 루프입니다.
		for(let i=1; i<=5; i++){
			// random(0, 9) 함수를 호출하여 0부터 9 사이의 랜덤한 숫자를 생성하고, 이를 value 문자열에 추가합니다.
			value += random(0, 9); // value = value + (random(0, 9))	
		}
	// 생성된 5자리 인증번호를 id가 'auth'인 요소의 텍스트로 설정합니다.
	document.getElementById("auth").innerText = value;	
}

/* refresh 함수는 페이지를 새로고침합니다. */
function refresh() {
	location.reload(); // location.reload() 메서드는 현재 페이지를 새로고침하여 autoNo() 함수를 다시 실행시킵니다.
}

/* goNaver 함수는 사용자가 네이버로 이동할지 확인한 후, 이동을 결정하는 함수입니다. */
function goNaver(){
	// confirm() 함수는 사용자에게 확인을 요청하는 팝업을 띄우고, 확인하면 true, 취소하면 false를 반환합니다.
	// 사용자가 확인 버튼을 클릭했을 경우 네이버로 이동합니다.
	if(confirm("정말로 네이버로 이동하겠습니까?")) location.href="https://www.naver.com"; 
	// location.href는 브라우저의 주소를 변경하여 다른 페이지로 이동하게 합니다.
}

</script>
</body>
</html>
