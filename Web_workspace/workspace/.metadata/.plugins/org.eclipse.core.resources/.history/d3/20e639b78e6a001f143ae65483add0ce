<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
    
    <%
    	// 데이터
    	String id = request.getParameter("id");
    
    	// DB 연결(싱글톤 생성 이후 연결)
    	MemberDAO 
    	
    	boolean exist = memberDAO.idExistId(id);
    	// id 가 있으면 true(사용 불가능),
    	// id 가 없으면 false(사용 가능)
    	
    	
    %>
    
    
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>ID 중복체크</title>
</head>
<body>

<%= if(){ %> 
	
<%}else{ %>

<%} %>
</body>
</html>