@charset "UTF-8";

body {
    background-color: #0a0a23 !important; /* 어두운 배경색으로 네온사인을 돋보이게 */
    font-family: 'Comic Sans MS', sans-serif !important;
    color: #d4a6ff;
    background-image: url('/path/to/your/neon-background.png'); /* 배경에 네온사인 느낌 추가 */
    background-size: cover;
}

/* 게시글 보기 레이아웃 */
.container {
    display: flex;
    justify-content: space-between;
    padding: 30px;
    width: 80%; /* 전체 영역을 80%로 확장 */
    margin: 0 auto; /* 화면 중앙에 배치 */
}

/* 왼쪽 섹션의 가로 길이가 내용에 따라 유동적으로 변화 */
.left-section, .right-section {
    background-color: #1f1f3b;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0px 0px 20px rgba(0, 255, 255, 0.7);
    color: #d4a6ff;
    flex: 1; /* 각 섹션이 동일한 비율로 공간을 차지 */
    margin-right: 20px;
}

.left-section {
    max-width: 65%; /* 왼쪽 섹션이 최대 65%까지 확장 가능 */
}

.right-section {
    max-width: 30%;
}

/* 정보 표시 div 스타일 */
#subject, #content, #name, #logtime, #hit {
    padding: 15px;
    border: 1px solid #8de4ff;
    border-radius: 5px;
    background-color: #14142f;
    margin-bottom: 15px;
    color: #fff;
    word-wrap: break-word; /* 글이 너무 길면 줄바꿈 처리 */
}

/* 레이블 스타일 */
label {
    font-weight: bold;
    display: block;
    margin-bottom: 5px;
    color: #ffdb3d;
}

/* 목록으로 돌아가는 버튼 스타일 */
input[type="button"] {
    width: 100%;
    padding: 15px;
    background-color: #ff46c7;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
    margin-top: 15px;
}

/* 버튼 hover 효과 */
input[type="button"]:hover {
    background-color: #ffdb3d;
}

/* h2 제목 스타일 */
h2 {
    cursor: pointer;
    text-align: center;
    color: #ffdb3d;
    font-size: 36px; /* 제목 크기를 크게 유지 */
}
