@charset "UTF-8";

/* body 스타일 */
body {
    margin: 0;
    padding: 0;
    background-color: #0a0a23 !important; /* 짙은 배경 */
    background-image: url('/path/to/neon-background.png'); /* 네온사인 느낌의 배경 */
    background-size: cover;
    color: #d4a6ff;
    font-family: 'Comic Sans MS', sans-serif !important;
    text-align: center;
}

/* 테이블 스타일 */
table {
    width: 600px;
    border-collapse: collapse;
    margin: 50px auto;
    padding: 20px;
    box-shadow: 0 0 20px rgba(0, 255, 255, 0.7);
    background-color: #1f1f3b !important;
    color: #d4a6ff;
}

/* 테이블 셀 스타일 */
td {
    padding: 15px;
    vertical-align: middle;
    font-weight: bold;
    color: #d4a6ff;
}

/* 첫 번째 열(레이블) 스타일 */
td:first-child {
    width: 120px;
    text-align: right;
    color: #ffdb3d;
}

/* 마지막 열(입력 필드) 스타일 */
td:last-child {
    text-align: left;
}

/* 중복체크 버튼 스타일 */
.check-btn {
    background-color: #ff46c7; /* 네온 핑크 */
    color: white;
    padding: 10px;
    border-radius: 5px;
    border: none;
    cursor: pointer;
    margin-top: 10px;
    font-size: 16px;
    width: 80%; /* 아이디 입력 필드와 동일한 폭 */
}

/* 버튼 hover 스타일 */
.check-btn:hover {
    background-color: #ffdb3d; /* 호버 시 네온 옐로우 */
}

/* 입력 필드 스타일 */
input[type="text"], input[type="password"], select {
    width: 80%;
    padding: 10px;
    border: 1px solid #8de4ff;
    border-radius: 4px;
    background-color: #14142f;
    color: #fff;
}

/* 버튼 스타일 */
input[type="button"], input[type="submit"], input[type="reset"] {
    width: 150px;
    padding: 15px;
    background-color: #ff46c7;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 16px;
}

input[type="button"]:hover, input[type="submit"]:hover, input[type="reset"]:hover {
    background-color: #ffdb3d;
}
