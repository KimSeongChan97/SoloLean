<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>연인과의 날짜 계산</title>
</head>
<body>
    <h1>연인과의 날짜 계산</h1>
    <div id="result"></div>

    <script>
        // 만난 날로부터 오늘까지 경과한 일수를 계산하는 함수
        function calculateDays() {
            // 사용자가 날짜를 입력받음 (기본 형식: "yyyy/mm/dd")
            const inputDate = prompt("연인과 만난 날을 입력하세요 (예시: yyyy/mm/dd):", "2022/01/01");
            
            // 입력 받은 날짜를 Date 객체로 변환
            const startDate = new Date(inputDate);
            
            // 오늘 날짜 가져오기
            const today = new Date();

            // 두 날짜 간의 차이 계산 (밀리초 단위)
            const timeDiff = today - startDate;
            
            // 밀리초를 일수로 변환
            const daysDiff = Math.floor(timeDiff / (1000 * 60 * 60 * 24));
            
            // 결과를 페이지에 출력
            document.getElementById('result').innerHTML = `오늘까지 우리는 ${daysDiff}일입니다.`;
        }

        // 페이지가 로드되면 날짜 계산 함수 호출
        window.onload = calculateDays;
    </script>
</body>
</html>
