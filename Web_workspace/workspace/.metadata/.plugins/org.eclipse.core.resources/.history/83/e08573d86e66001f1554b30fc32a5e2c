/* 전역 스타일 설정: 페이지 전체의 폰트, 마진, 패딩, 배경색을 설정합니다. */
body {
    font-family: Arial, sans-serif; /* Arial 폰트를 기본 폰트로 사용합니다. */
    margin: 0; /* 페이지의 기본 마진을 제거하여 여백을 없앱니다. */
    padding: 0; /* 페이지의 기본 패딩을 제거하여 여백을 없앱니다. */
    background-color: gray; /* 페이지 배경색을 회색으로 설정합니다. */
}

/* 메인 콘텐츠 영역 스타일 */
main {
    display: block; /* 메인 콘텐츠를 블록 요소로 설정하여 차지할 수 있는 최대 너비를 차지하도록 합니다. */
    justify-content: space-around; /* flexbox에서 자식 요소들을 공간을 기준으로 배치합니다. */
    padding: 20px; /* 메인 콘텐츠에 20px의 패딩을 추가하여 여백을 줍니다. */
}

/* 지도 섹션 스타일 */
section#map {
    width: 100%; /* 섹션의 너비를 부모 요소의 100%로 설정합니다. */
    height: 300px; /* 고정된 높이 350px로 설정하여 지도의 높이를 지정합니다. */
}

/* 헤더 내비게이션 바 스타일 */
header nav {
    display: flex; /* 플렉스 박스를 사용하여 내비게이션 바의 아이템들을 배치합니다. */
    justify-content: space-between; /* 내비게이션 아이템들을 양 끝에 배치합니다. */
    background-color: #333; /* 내비게이션 바의 배경색을 어두운 회색(#333)으로 설정합니다. */
    padding: 10px; /* 내비게이션 바에 10px의 패딩을 추가하여 여백을 줍니다. */
}

/* 내비게이션 바의 리스트 스타일 */
header nav ul {
    list-style: none; /* 리스트 스타일을 제거하여 불릿 포인트를 없앱니다. */
    display: flex; /* 리스트 항목들을 가로로 나열합니다. */
    margin: 0; /* 리스트의 기본 마진을 제거합니다. */
    padding: 0; /* 리스트의 기본 패딩을 제거합니다. */
}

/* 내비게이션 바의 리스트 항목 스타일 */
header nav ul li {
    margin: 0 15px; /* 각 리스트 항목 사이에 좌우로 15px의 간격을 줍니다. */
}

/* 내비게이션 바의 링크 스타일 */
header nav ul li a {
    color: white; /* 링크의 텍스트 색상을 흰색으로 설정합니다. */
    text-decoration: none; /* 링크의 밑줄을 제거하여 깔끔하게 만듭니다. */
}

/* 내비게이션 바의 로고 이미지 스타일 */
.navbar-brand img {
    height: 40px; /* 로고 이미지의 높이를 40px로 설정합니다. */
}

/* 내비게이션 링크에 포함된 이미지 스타일 */
.nav-link img {
    height: 30px; /* 내비게이션 링크 이미지의 높이를 30px로 설정합니다. */
}

/* 뒤로가기 버튼 스타일 */
#back-button {
    position: fixed; /* 화면에 고정되어 스크롤해도 위치가 변하지 않도록 설정합니다. */
    right: 20px; /* 화면의 오른쪽에서 20px 떨어진 위치에 버튼을 배치합니다. */
    bottom: 20px; /* 화면의 아래쪽에서 20px 떨어진 위치에 버튼을 배치합니다. */
    background-color: rgba(0, 0, 0, 0.5); /* 반투명한 검은색 배경을 설정하여 배경을 살짝 가립니다. */
    padding: 10px; /* 버튼에 10px의 패딩을 추가하여 클릭 영역을 넓힙니다. */
    border-radius: 50%; /* 버튼을 둥글게 만들어 원형으로 표시되도록 설정합니다. */
    cursor: pointer; /* 버튼에 마우스 포인터가 올라갈 때 손 모양이 나오도록 설정합니다. */
}

/* 뒤로가기 버튼의 이미지 스타일 */
#back-button img {
    width: 40px; /* 이미지의 너비를 40px로 설정합니다. */
    height: 40px; /* 이미지의 높이를 40px로 설정합니다. */
}

/* 푸터 스타일 */
footer {
    text-align: center; /* 푸터 내 텍스트를 가운데 정렬합니다. */
    padding: 20px; /* 푸터에 20px의 패딩을 추가하여 여백을 줍니다. */
    background-color: #333; /* 푸터의 배경색을 어두운 회색(#333)으로 설정합니다. */
    color: white; /* 푸터의 텍스트 색상을 흰색으로 설정합니다. */
}

/* 내비게이션 바의 배경 스타일 */
.navbar {
    background: linear-gradient(135deg, #7f00ff, #e100ff); /* 135도 각도로 연보라색 그라데이션을 설정합니다. */
    border-radius: 10px; /* 내비게이션 바의 모서리를 10px로 둥글게 만듭니다. */
}

/* 내비게이션 바의 브랜드 로고 스타일 */
.navbar-brand {
    font-weight: bold; /* 로고의 폰트를 굵게 설정합니다. */
    color: #fff !important; /* 로고의 텍스트 색상을 흰색으로 설정하고 중요한 속성으로 지정합니다. */
}

/* 내비게이션 링크 스타일 */
.nav-link {
    color: #ffffff !important; /* 링크 텍스트 색상을 흰색으로 설정하고 중요한 속성으로 지정합니다. */
}

/* 내비게이션 링크 호버 스타일 */
.nav-link:hover {
    color: #e100ff; /* 호버 시 링크 텍스트 색상이 밝은 보라색으로 변경됩니다. */
    text-shadow: 0 0 10px rgba(225, 0, 255, 0.7); /* 호버 시 텍스트에 빛나는 효과를 추가합니다. */
}

/* 활성화된 내비게이션 링크 스타일 */
.nav-link.active {
    font-weight: bold; /* 활성화된 링크를 굵은 폰트로 표시합니다. */
    text-decoration: underline; /* 활성화된 링크에 밑줄을 추가합니다. */
}

/* 내비게이션 바의 브랜드 로고 스타일 (중복 정의) */
.navbar-brand {
    font-weight: bold; /* 로고의 폰트를 굵게 설정합니다. */
    color: #fff !important; /* 로고의 텍스트 색상을 흰색으로 설정하고 중요한 속성으로 지정합니다. */
}

/* 컨테이너 스타일 */
.container {
    background-color: rgba(255, 255, 255, 0.2); /* 반투명한 흰색 배경을 설정합니다. */
    padding: 20px; /* 컨테이너에 20px의 패딩을 추가하여 여백을 줍니다. */
    border-radius: 10px; /* 컨테이너의 모서리를 10px로 둥글게 만듭니다. */
    margin-top: 20px; /* 컨테이너 상단에 20px의 마진을 추가하여 다른 요소들과의 간격을 줍니다. */
}

/* 폼 컨트롤 스타일 */
.form-control {
    background-color: rgba(255, 255, 255, 0.1); /* 매우 투명한 흰색 배경을 설정합니다. */
    border: 1px solid #fff; /* 흰색 실선 테두리를 설정합니다. */
    color: #fff; /* 폼 텍스트 색상을 흰색으로 설정합니다. */
}

/* 버튼 스타일 */
.btn {
    background-color: #7f00ff; /* 버튼 배경색을 보라색으로 설정합니다. */
    border: none; /* 버튼 테두리를 제거합니다. */
}

/* 버튼 호버 스타일 */
.btn:hover {
    background-color: #e100ff; /* 호버 시 버튼 배경색이 밝은 보라색으로 변경됩니다. */
    box-shadow: 0 0 10px rgba(225, 0, 255, 0.7); /* 호버 시 버튼에 빛나는 효과를 추가합니다. */
    transform: scale(1.05); /* 호버 시 버튼이 약간 커지도록 애니메이션을 적용합니다. */
}

/* 카드 스타일 */
.card {
    background-color: white; /* 카드 배경색을 흰색으로 설정합니다. */
    padding: 20px; /* 카드에 20px의 패딩을 추가하여 여백을 줍니다. */
    margin: 20px 0; /* 카드 상하단에 20px의 마진을 추가하여 다른 요소들과의 간격을 줍니다. */
    border-radius: 10px; /* 카드의 모서리를 10px로 둥글게 만듭니다. */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* 카드에 그림자를 추가하여 입체감을 줍니다. */
    transition: box-shadow 0.3s ease; /* 카드 호버 시 그림자 효과의 애니메이션 속도를 설정합니다. */
}

/* 카드 호버 스타일 */
.card:hover {
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3); /* 호버 시 카드의 그림자를 더 깊게 만들어 입체감을 강화합니다. */
}

/* 페이드 인 애니메이션 초기 상태 */
.fade-in {
    opacity: 0; /* 초기 상태에서 요소를 투명하게 설정합니다. */
    transform: translateY(20px); /* 요소를 초기 위치에서 아래로 20px 이동시킵니다. */
    transition: opacity 0.5s ease-out, transform 0.5s ease-out; /* 요소가 나타날 때의 애니메이션 속도와 효과를 설정합니다. */
}

/* 페이드 인 애니메이션 활성 상태 */
.fade-in.visible {
    opacity: 1; /* 요소를 완전히 불투명하게 설정하여 보이도록 합니다. */
    transform: translateY(0); /* 요소를 원래 위치로 이동시켜 제자리에 오도록 합니다. */
}

/* 섹션 요소 스타일 */
section {
    padding: 40px 0; /* 섹션 요소의 상하단에 40px의 패딩을 추가하여 여백을 줍니다. */
}

/* 푸터 스타일 */
footer {
    position: relative; /* 푸터를 기준으로 내부 요소의 위치를 설정할 수 있도록 합니다. */
    background-color: #333; /* 푸터의 배경색을 어두운 회색(#333)으로 설정합니다. */
    color: white; /* 푸터 텍스트 색상을 흰색으로 설정합니다. */
    padding: 20px; /* 푸터에 20px의 패딩을 추가하여 여백을 줍니다. */
    text-align: center; /* 푸터 내 텍스트를 가운데 정렬합니다. */
}

/* 푸터 상단 톱니형 디자인 스타일 */
footer:before {
    content: ""; /* 가상 요소로 추가적인 콘텐츠 없이 스타일만 적용합니다. */
    position: absolute; /* 푸터 내에서 가상 요소의 위치를 절대적으로 설정합니다. */
    top: -20px; /* 가상 요소의 상단 위치를 푸터 위쪽으로 20px 이동시킵니다. */
    left: 0; /* 가상 요소를 왼쪽에 고정합니다. */
    width: 100%; /* 가상 요소의 너비를 푸터 전체 너비로 설정합니다. */
    height: 20px; /* 가상 요소의 높이를 20px로 설정합니다. */
    background: linear-gradient(135deg, transparent 25%, #333 25%, #333 50%, transparent 50%); /* 135도 각도의 톱니형 그라데이션을 적용합니다. */
    background-size: 40px 40px; /* 톱니형 패턴의 크기를 40px x 40px로 설정합니다. */
}

.info-popup {
    position: fixed;
    bottom: -200px;
    left: 50%;
    transform: translateX(-50%);
    width: 90%;
    max-width: 400px;
    background-color: white;
    border: 1px solid #ccc;
    border-radius: 10px;
    padding: 20px;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    transition: bottom 0.5s;
    z-index: 1000;
}

.info-popup.visible {
    bottom: 20px;
}

.info-popup button {
    width: 100%;
}

.directions-panel {
    position: fixed;
    left: -400px;
    top: 0;
    width: 400px;
    height: 100%;
    background-color: white;
    border-right: 1px solid #ccc;
    overflow-y: scroll;
    transition: left 0.5s;
    z-index: 1000;
}

.directions-panel.visible {
    left: 0;
}

/* X 버튼 스타일 */
.close-button {
    position: absolute; /* 절대 위치 설정 */
    top: 10px; /* 팝업 상단에서 10px 아래 */
    right: 10px; /* 팝업 오른쪽에서 10px 왼쪽 */
    width: 20px; /* 버튼 너비 */
    height: 20px; /* 버튼 높이 */
    cursor: pointer; /* 마우스 포인터를 올리면 클릭 모양으로 변경 */
}

/* 팝업창 숨기기 */
.info-popup.hidden {
    display: none;
}
