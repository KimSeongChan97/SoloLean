@charset "UTF-8";

@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap');

/* 전체 페이지의 어두운 배경 */
body {
    background-color: #0a0a0a; /* 매우 어두운 배경색 */
    color: #f2f2f2; /* 기본 텍스트 색상 */
    font-family: 'Orbitron';
    overflow: hidden;
}

/* 결과 텍스트 스타일링 */
body > div {
    text-align: center;
    font-size: 2em;
    color: #0affff; /* 결과 텍스트 네온 색상 */
    text-shadow: 
        0 0 5px #0affff, 
        0 0 10px #0affff, 
        0 0 20px #00ffff, 
        0 0 40px #00ffff; /* 네온사인 효과를 위한 텍스트 그림자 */
    border: 2px solid #0affff; /* 형광 테두리 */
    padding: 20px;
    box-shadow: 0 0 10px #0affff, 0 0 20px #00ffff; /* 네온사인처럼 보이는 박스 그림자 */
    background-color: transparent; /* 배경 투명 */
    border-radius: 20px;
    display: inline-block; /* 결과 텍스트를 박스 내에서만 중앙에 배치 */
    margin-bottom: 20px; /* 버튼과의 간격 */
}

/* Back 버튼 스타일 */
form {
    text-align: center;
    margin-top: 20px;
}

input[type="submit"] {
    padding: 10px 20px;
    background-color: #0affff; /* 형광 버튼 */
    color: #000;
    border: none;
    font-size: 1.2em;
    font-family: 'Orbitron';
    cursor: pointer;
    border-radius: 5px;
    box-shadow: 0 0 10px #0affff, 0 0 20px #00ffff;
    margin-top: 10px; /* 결과 텍스트와 버튼 간의 간격 조정 */
}

input[type="submit"]:hover {
    box-shadow: 0 0 15px #0affff, 0 0 30px #00ffff;
}

