@charset "UTF-8";  

/* 기본적인 전반적인 설정 */
body {
    font-family: 'Poppins', sans-serif; /* 세련된 폰트로 변경 */
    background: linear-gradient(to bottom right, #f0f4f8, #ffffff); /* 부드러운 그라디언트 배경 */
    color: #333;
    margin: 0;
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 100vh;
}

/* 홈 버튼 스타일 */
.home-container {
    width: 100%;
    text-align: center;
    margin-bottom: 20px;
}

a {
    text-decoration: none;
    color: #fff;
    background-color: #007bff; /* 밝고 생동감 있는 파란색 */
    padding: 12px 20px;
    border-radius: 30px;
    font-size: 18px;
    font-weight: bold;
    transition: all 0.3s ease;
    box-shadow: 0 8px 15px rgba(0, 123, 255, 0.2); /* 입체적인 그림자 */
}

a:hover {
    background-color: #0056b3;
    transform: translateY(-3px);
    box-shadow: 0 12px 20px rgba(0, 123, 255, 0.4);
}

/* 테이블 컨테이너 */
.table-container {
    width: 80%;
    max-height: 70vh;
    overflow-y: auto;
    background: rgba(255, 255, 255, 0.9); /* 약간 투명한 흰색 배경 */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
    border-radius: 10px;
    padding: 20px;
    margin-top: 20px;
}

/* 테이블 스타일 */
table {
    width: 100%;
    border-collapse: collapse; /* 중복된 경계선을 제거 */
    background-color: #fff; /* 흰색 배경 */
    border-radius: 10px;
    overflow: hidden;
}

th, td {
    padding: 15px;
    text-align: center;
    font-size: 16px;
    transition: background-color 0.3s ease; /* 배경색 전환이 부드럽게 적용 */
}

/* 테이블 헤더 스타일 */
th {
    background-color: #007bff; /* 파란색 헤더 */
    color: white;
    text-transform: uppercase;
    letter-spacing: 1px; /* 글자 간격 넓히기 */
    font-weight: bold;
    border-bottom: 2px solid #0056b3;
}

/* 테이블 데이터 스타일 */
td {
    border-bottom: 1px solid #ddd;
    color: #555;
}

/* 짝수 행 스타일 */
tr:nth-child(even) {
    background-color: #f9f9f9; /* 연한 회색 배경 */
}

/* 행에 마우스를 올렸을 때 */
tr:hover {
    background-color: #e0f7fa; /* 연한 파란색으로 강조 */
}

/* 이미지 스타일 */
img {
    border-radius: 12px;
    transition: transform 0.5s ease, box-shadow 0.5s ease;
    display: block;
    margin: 0 auto;
    width: 80px;
    height: 80px;
}

img:hover {
    transform: scale(1.2); /* 확대 효과 */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* 그림자 효과 */
}

/* 링크 내부의 이미지에 대해 포커스 스타일 제거 */
a img:focus {
    outline: none;
}

/* 부드러운 전환 효과 */
tr:hover img {
    transform: scale(1.3); /* 행에 마우스가 올라갔을 때 이미지 확대 */
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
}
